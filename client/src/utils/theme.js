import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  // Define your theme configuration here
  spacing: 8, // Set the desired spacing value
  // Other theme options...
});

export default theme;
